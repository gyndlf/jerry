TODO:
    - Fix algorithm so that it can play a card from another suit if it runs out (Push through global card values)
    - Create reward function / scoring
    - If there is a kitty, save this and add to the first that get points